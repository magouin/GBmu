#ifndef HEADER_HPP
# define HEADER_HPP

class HEADER {
	private:
		/* parameters */
	public:
		HEADER(/* args */);
		HEADER(const HEADER & cp);
		~HEADER(/* args */);
		HEADER & operator=(const HEADER & cp);
};

#endif
